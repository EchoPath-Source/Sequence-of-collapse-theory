# Engramon Scale — Concept Note

**Status:** Concept extraction / needs formalization  
**Primary source:** `engramon_scale.docx` plus PNT substrate discussion  
**Claim level:** Speculative SoCT/PNT concept; not yet a formal physical quantity.

---

## Purpose

The Engramon is a proposed persistent state modification in the Planck-scale or memory-field substrate.

In the PNT framing, the Engramon becomes a way to name the smallest persistent unit of collapse-memory residue:

```text
Engramon = persistent Planck-cell state modification left by a collapse/nucleation event
```

---

## Relationship to PNT

PNT proposes that every successful or failed nucleation modifies the entanglement structure of nearby Planck cells.

The Engramon concept can be treated as the discrete or quasi-discrete record unit of that modification.

Possible relation:

```text
M(x,t) = sum or integral over Engramon-like residue events
```

---

## Research Use

The Engramon concept may support:

- memory-kernel formalization;
- collapse-history quantization;
- PNT interpretation of Planck-cell state changes;
- SPARC memory timescale modeling;
- CIH junction-surface residue modeling;
- future simulation of memory-field accumulation.

---

## Required Formalization

Before publication use, define:

1. whether an Engramon is discrete, continuous, or effective;
2. what physical state variable it modifies;
3. how it decays or decoheres;
4. whether it has spatial extent;
5. how multiple Engramons superpose;
6. whether it conserves energy/information;
7. how it connects to observable quantities.

---

## Suggested Future Equation Direction

```text
M(x,t) = sum_i e_i * K(|x - x_i|, t - t_i)
```

Where:

- `e_i` is an Engramon-like event weight;
- `K` is a memory kernel;
- `(x_i, t_i)` is the collapse/nucleation event location;
- `M(x,t)` is the effective memory field.

---

## Repository Links

Related files:

```text
papers/cosmology/pnt/planck-nucleation-theory-master-note.md
papers/pnt-dark-energy-hubble-window/working-draft-v0-1.md
experiments/cosmology/sparc-analysis-plan.md
```

---

## Paper-Safe Language

Use:

> We introduce the Engramon as a provisional bookkeeping concept for persistent collapse-memory residue.

Avoid:

> Engramons are proven particles or confirmed physical entities.
